---
export interface Props {
  title: string
  size: string
}

const { title, size } = Astro.props
---

<div title={title} class='title relative grid place-content-center w-14 h-10'>
  <img src={`/${title.toLowerCase()}.svg`} width={size} height={size} alt={`Logo ${title}`} />
</div>
